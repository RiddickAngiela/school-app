

<h1>Students List</h1>

<div id="students" class='table table-striped table-bordered table-hover'>
  <% @students.each do |student| %>
    <%= render student %>
    <p>
      <%= link_to "Show this student", student %>
    </p>
  <% end %>
</div>

<%= link_to "New student", new_student_path, class:"btn btn-secondary" %>
